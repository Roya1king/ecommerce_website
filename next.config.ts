import type { NextConfig } from "next";

/** @type {import('next').NextConfig} */
const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com',"127.0.0.1", "localhost",'ecommercewebsiteapi-production.up.railway.app','ecommerce-website-api-zvg2.onrender.com','res.cloudinary.com','res.cloudinary.com',],
  },
};

export default nextConfig;
